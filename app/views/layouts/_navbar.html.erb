<nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">MyMarket</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Home', root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= (link_to show_user, edit_user_registration_path, class: "nav-link") if user_signed_in? %>
        </li>
        <% if user_signed_in? %>
          <%= render 'layouts/nav_menu' %>
        <% end %>
      </ul>
      <%= search_form_for @q, url: search_path, class: "d-flex me-2" do |f| %>
        <%= f.search_field :name_cont, class: "form-control me-2" %>
        <%= f.submit "Search", class: "btn btn-outline-success" %>
      <% end %>
      <% if user_signed_in? %>
        <%= link_to 'Keluar', destroy_user_session_path, method: :delete, class: "btn btn-outline-primary rounded-pill" %>
      <% else %>
        <%= link_to 'Daftar', new_user_registration_path, class: "btn btn-outline-primary rounded-pill" %>
        <%= link_to 'Masuk', new_user_session_path, class: "btn btn-primary rounded-pill ms-2" %>
      <% end %>
    </div>
  </div>
</nav>
