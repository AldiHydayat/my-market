<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3>Your order is on delivery</h3>
    <h4>Order Details</h4>
    <% @order.order_details.each do |od| %>
      <p>
        product: <%= od.product.name %>
        <br>
        quantity: <%= od.quantity %>
      </p>
    <% end %>
    <p>Total : <%= number_to_currency(@order.total_price, unit: "Rp. ") %></p>
    <small>*Please confirm an order after recieve your order</small>
  </body>
</html>
